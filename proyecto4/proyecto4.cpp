#include <iostream>
#include <string>

using namespace std;

int main()
{
    string nombre;
    cout << "Ejemplo de usio de cadena(string)" << endl;

    cout << "Ingrese su nombre por favor" << endl;
    cin >> nombre;
    cout << "Ejemplo de uso de cadena (string)" << endl;
         << nombre << endl;
    return 0;
}
